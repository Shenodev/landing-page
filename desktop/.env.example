# ELECTRON_WEB_URL defaults to https://shenodev.dpdns.org in production builds.
# Override locally to point at a dev server or preview deployment:
ELECTRON_WEB_URL=http://localhost:3000
# https://shenodev.dpdns.org (production)
NODE_ENV=production